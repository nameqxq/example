server:
  port: 9999
  tomcat:
    uri-encoding: utf-8

spring:
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physicalStrategy: test.quxiqi.sharding.sphere.config.LaimiNamingStrategy
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  shardingsphere:
    datasource:
      name: ds
      ds:
        driverClassName: com.mysql.jdbc.Driver
        jdbcUrl: jdbc:mysql://101.236.55.211:22351/test?useUnicode=true&characterEncoding=UTF-8
        username: laimi_develop
        type: com.zaxxer.hikari.HikariDataSource
        password: TSzIFJHThBBqwwwM
    sharding:
      tables:
        example:
          tableStrategy:
            standard:
              shardingColumn: id
              preciseAlgorithmClassName: test.quxiqi.sharding.sphere.config.sharding.ExampleTableStrategy
          keyGenerator:
            type: SNOWFLAKE
            column: id
    props:
      sql.show: true